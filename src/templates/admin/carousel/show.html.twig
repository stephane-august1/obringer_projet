{% extends 'base.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-12">
    <h1 class="text-center">Carousel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ carousel.name }}</td>
            </tr>
            <tr>
                <th>Srcimagecarousel</th>
                <td>{{ carousel.srcimagecarousel }}</td>
            </tr>
            <tr>
                <th>Descriptioncarousel</th>
                <td>{{ carousel.descriptioncarousel }}</td>
            </tr>
            <tr>
                <th>Titlecarousel</th>
                <td>{{ carousel.titlecarousel }}</td>
            </tr>
            <tr>
                <th>Categories</th>
                <td>{{ carousel.categories }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ carousel.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
</div></div>
<div class="row">
<div class="col-12">
    <a href="{{ path('carousel_index') }}">back to list</a>

    <a href="{{ path('carousel_edit', {'id': carousel.id}) }}">edit</a>

    {{ include('admin/carousel/_delete_form.html.twig') }}
    <br>
    <br>
    <br>
    <br>
    </div><br>
    <br>
    <br>
    </div>
    </div>
{% endblock %}
