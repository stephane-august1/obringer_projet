{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container">
    <h1>Edit User</h1>

    {{ include('user/avis/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_show', {'id': app.user.id}) }}">back to list</a>

    {{ include('user/avis/_delete_form.html.twig') }}
</div>
{% endblock %}
